import React from 'react'
import ReactDOM from 'react-dom/client'
import RoutePlanner from './route-planner.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RoutePlanner />
  </React.StrictMode>,
)
